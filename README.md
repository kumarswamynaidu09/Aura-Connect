# Aura-Connect
A user-owned AI identity and memory layer that travels across applications. Aura Connect lets users securely store their context, control which AI agents can access it, and revoke access anytime — with Monad providing the ownership, permissions, verification, and micropayment layer.
